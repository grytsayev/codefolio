import styled from 'styled-components';

export const StyledHomeContainer = styled.div`
    width: calc(100% - 200px);
    height: 100%;
    overflow: hidden;
    display: flex;
    flex-direction: row;
    gap: 5%;
    padding: 100px;
`;
