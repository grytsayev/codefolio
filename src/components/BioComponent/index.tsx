import React from 'react';
import { StyledBioComponentContainer } from './styles';

export const BioComponent = () => {
    return (
        <StyledBioComponentContainer>
            <span>Title</span>
            <h2>Subtitle</h2>
        </StyledBioComponentContainer>
    );
};
