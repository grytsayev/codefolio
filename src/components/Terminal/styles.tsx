import { ReactTerminal } from 'react-terminal';
import styled from 'styled-components';

export const StyledTerminalContainer = styled.div`
    width: 50%;
    height: 500px;
`;

export const StyledTerminal = styled(ReactTerminal)``;
